{
  "name": "chatcircle-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chat Circles agent 数据取送 MCP server（取数走现有 exports API，报告回传 reports 集合）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "selftest": "node server.js --selftest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "unzipper": "^0.12.5",
    "zod": "^4.4.3"
  }
}
