# backend/tests/

服务端集成测试（L3）占位目录。

运行方式约定（test-plan §3）：每次测试启动独立 PocketBase 实例（临时数据目录 → 顺序应用全部迁移 → 注入 fixture），通过 HTTP 或 JS SDK 调用真实 API，**只断言 API 行为与数据终态，不测内部函数**。

首批套件随 M1/M2 落地：机构隔离越权套件（AC-03，CI 必过）、名额事务硬校验（AC-08）、状态迁移矩阵（AC-07）、幂等提交（AC-20）。
